node("docker") {
    docker.withRegistry('<<https://hub.docker.com>>', '<<alandrade2002>>') {
    
        
        stage "build"
        def app = docker.build "your-project-name"
    
        stage "publish"
        app.push 'master'
        app.push "${commit_id}"
    }
}
